import * as THREE from 'three';
import { EditorState } from './state';
export declare const setupInteraction: (renderer: THREE.WebGLRenderer, camera: THREE.Camera, scene: THREE.Scene, state: EditorState) => {
    raycaster: THREE.Raycaster;
    mouse: THREE.Vector2;
};
//# sourceMappingURL=interaction.d.ts.map